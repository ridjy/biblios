{% extends 'base.html.twig' %}

{% block title %}Catalogue{% endblock %}

{% block body %}
    <section class="py-5">
        <div class="container px-4 px-lg-5 mt-5">
            <button class="btn btn-primary" onclick="window.location.href='{{ path('app_admin_book_new') }}'">Ajouter un livre</button>
            <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
                {% for book in books %}
                    <div class="col mb-5">
                        <div class="card h-100">
                            <!-- Détails du livre-->
                            <div class="card-body p-4">
                                <div class="text-center">
                                    <!-- Titre du livre-->
                                    <h5 class="fw-bolder">{{ book.title }}</h5>
                                    <!-- Auteurs du livre-->
                                    {{ book.author.name }}
                                </div>
                            </div>
                            <!-- Action-->
                            <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                                <div class="text-center">
                                    <a class="btn btn-outline-dark mt-auto" href="{{ path('app_admin_book_show', {id: book.id}) }}">Voir</a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <p>Aucun livre actuellement</p>
                {% endfor %}
            </div>
        </div>
    </section>

{% endblock %}
